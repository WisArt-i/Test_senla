#include <iostream>
using namespace std;
int main()
{
setlocale(LC_ALL, "Russian");
char s[],f[],buf[];
int slovo, count = 0;
cout << ("Введите предложение:") << endl;
cin.get(s);
  int i = 0;
  while (s[i] == ' ' && s[i] != '\0')
    i++;
  slovo = 0;
  while (s[i] != '\0') {
    if (s[i] != ' ' && slovo == 0)
    {
      slovo = 1;
      f[count++] = i;
    }
    else if (s[i] == ' ')
      slovo = 0;
    i++;
  }
  cout << "Количество слов в строке " << count;
  for (int j = 0; j < count-1; j++) {
  if (s[f[j]] == s[f[j+1]]) {
  
  }else if (s[f[j]] < s[f[j+1]]){
  
  }
  }
  cin.get(); cin.get();
  return 0;
 
